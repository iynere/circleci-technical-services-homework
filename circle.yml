general:
  branches:
    ignore:
      - gh-pages

dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
  override:
    - sudo yarn

deployment:
  staging:
    branch: master
    commands:
      - ./deploy.sh buildd