general:
  branches:
    ignore:
      - gh-pages

dependencies:
  override:
    - npm install

test:
  post:
    - npm run test

deployment:
  staging:
    branch: master
    commands:
      - ./deploy.sh build